node_modules
.next
out
# Real environment files are never committed; only the placeholder template is.
.env
.env.*
.env*.local
!.env.example
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.tsbuildinfo
codex-dev-server*.log

# Agent guidance files `next dev` (16.3+) writes into the project root.
AGENTS.md
CLAUDE.md
test-results/
playwright-report/
.customizer-e2e.json

# Explorer duplicates ("Copy of x", "x - Copy") — these slipped a whole
# built .next tree into the repo once already.
* - Copy/
* - Copy (*)/
Copy of *


# Supplied source archives / transfer artifacts (never part of the app)
app.zip
*.zip
